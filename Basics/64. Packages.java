import java.util.Scanner;
//import java.util.*;

public class Practice2{
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        // java.util.Scanner sc = new java.util.Scanner(System.in);
        int a = sc.nextInt();
        System.out.println("This is my scanner taking input as " + a);
    }
}

// javac -d . * . java 
/* By puting the above command if at the top 
 * there is a package then its class folder will
 * be created and that package command should be 
 * there in all the code so that they could be 
 * in the same package class folder.
 * Make sure to write the package corectly.
 * This will creatre class folder which can
 * be used by other OS.
*/
